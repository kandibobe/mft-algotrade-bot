# Быстрый Старт - Stoic Citadel
# ===============================

**Это краткое руководство для тех, кто хочет быстро начать.**

Полная документация: `DEPLOYMENT.md`

---

## ⚡ 5-Минутная Настройка (Dry-Run)

### Шаг 1: Создайте .env файл

```bash
# Скопируйте шаблон
cp .env.example .env

# Для сухого прогона (dry-run) можно оставить API ключи пустыми
# Бот будет торговать виртуальными деньгами
```

### Шаг 2: Проверьте конфигурацию

```bash
# Установите необходимые зависимости
python3 scripts/validate_config.py
```

### Шаг 3: Настройте инфраструктуру

```bash
# Это создаст все директории и установит Docker образы (5-10 минут)
./scripts/deploy.sh --setup
```

### Шаг 4: Скачайте данные

```bash
# Скачает 2 года исторических данных (10-15 минут)
./scripts/deploy.sh --data
```

### Шаг 5: Запустите бота

```bash
# Запуск в dry-run режиме (виртуальные деньги)
./scripts/citadel.sh trade

# Откройте дашборд в браузере:
# http://127.0.0.1:3000
```

**Готово!** Бот торгует виртуальными деньгами.

---

## 🔑 Настройка для Live Trading

⚠️ **ВНИМАНИЕ**: Только после тестирования в dry-run режиме!

### 1. Получите API ключи от Binance

**Пошаговая инструкция:**

1. Откройте https://www.binance.com/
2. Войдите в аккаунт
3. Наведите на иконку профиля → **API Management**
4. Нажмите **Create API** → **System Generated**
5. Введите Label: `StoicCitadel`
6. Подтвердите через 2FA
7. **Сохраните оба ключа:**
   ```
   API Key: ваш_публичный_ключ
   Secret Key: ваш_секретный_ключ (показывается 1 раз!)
   ```

**Настройка разрешений:**
- ✅ Enable Reading
- ✅ Enable Spot & Margin Trading
- ❌ Enable Withdrawals (НИКОГДА!)

**IP Whitelist (рекомендуется):**
```bash
# Узнайте IP вашего сервера
curl ifconfig.me

# Добавьте этот IP в API restrictions на Binance
```

### 2. Добавьте ключи в .env

```bash
nano .env

# Вставьте ваши ключи:
BINANCE_API_KEY=ваш_api_key_здесь
BINANCE_API_SECRET=ваш_secret_key_здесь
```

### 3. Настройте Telegram бот (опционально, но рекомендуется)

**За 2 минуты:**

1. Откройте Telegram → найдите `@BotFather`
2. Отправьте: `/newbot`
3. Имя бота: `Stoic Citadel Trading Bot`
4. Username: `StoicCitadel_bot` (должен заканчиваться на _bot)
5. Скопируйте токен (формат: `123456789:ABCdefGHI...`)

6. Найдите `@userinfobot` → отправьте `/start`
7. Скопируйте ваш Chat ID (цифры)

8. Добавьте в .env:
```bash
TELEGRAM_TOKEN=123456789:ABCdefGHIjklMNO...
TELEGRAM_CHAT_ID=123456789
```

### 4. Включите Live Trading

```bash
# Откройте конфиг
nano user_data/config/config_production.json

# Измените dry_run на false:
{
  "dry_run": false,
  "exchange": {
    "name": "binance",
    "key": "${BINANCE_API_KEY}",
    "secret": "${BINANCE_API_SECRET}"
  }
}
```

### 5. Запустите Live Trading

```bash
./scripts/citadel.sh trade-live

# ⚠️ Бот подтвердит, что вы понимаете риски
# Вы должны ввести: I UNDERSTAND THE RISKS
```

---

## 📊 Быстрая Проверка

### Проверка подключения к бирже

```bash
docker-compose run --rm freqtrade list-markets --exchange binance
```

**Ожидается:** Список торговых пар (BTC/USDT, ETH/USDT...)

### Проверка баланса

```bash
docker-compose run --rm freqtrade show-balance \
  --config /freqtrade/user_data/config/config_production.json
```

**Dry-run:** Виртуальный баланс из конфига
**Live:** Реальный баланс на бирже

### Проверка Telegram

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_TOKEN>/sendMessage" \
  -d "chat_id=<YOUR_CHAT_ID>" \
  -d "text=Test from Stoic Citadel"
```

Замените `<YOUR_TOKEN>` и `<YOUR_CHAT_ID>` на ваши значения.

---

## 🎯 Основные Команды

### Управление ботом

```bash
./scripts/citadel.sh start      # Запустить все сервисы
./scripts/citadel.sh stop       # Остановить всё
./scripts/citadel.sh status     # Статус сервисов
./scripts/citadel.sh logs       # Просмотр логов
./scripts/citadel.sh trade      # Dry-run торговля
./scripts/citadel.sh trade-live # Live торговля (осторожно!)
```

### Telegram команды

В Telegram отправьте боту:

```
/status   - Открытые позиции
/profit   - Прибыль/убыток
/balance  - Баланс на бирже
/daily    - Дневная статистика
/stop     - Остановить торговлю
/forceexit all - Экстренное закрытие всех позиций
```

### Мониторинг

```bash
# Логи в реальном времени
./scripts/citadel.sh logs freqtrade

# Дашборд
http://127.0.0.1:3000

# Jupyter Lab (исследования)
http://127.0.0.1:8888  (token: stoic2024)
```

---

## 🚨 Экстренная Остановка

### Panic Button

```bash
# Немедленная остановка
./scripts/citadel.sh stop

# Или через Docker
docker-compose down
```

### Через Telegram

```
/forceexit all
```

---

## 📁 Структура Файлов

```
stoic-citadel/
├── .env                          ← ВАШИ API КЛЮЧИ (не коммитить!)
├── docker-compose.yml            ← Инфраструктура
│
├── user_data/
│   ├── config/
│   │   └── config_production.json  ← Настройки бота
│   ├── strategies/
│   │   └── StoicStrategyV1.py      ← Торговая стратегия
│   └── data/                       ← Исторические данные
│
└── scripts/
    ├── citadel.sh                ← Главный скрипт управления
    ├── deploy.sh                 ← Скрипт деплоя
    └── validate_config.py        ← Проверка настроек
```

---

## ❓ Частые Вопросы

### Q: Нужны ли мне API ключи для тестирования?

**A:** Нет! Для dry-run режима ключи не нужны. Бот будет торговать виртуальными деньгами.

### Q: Как долго тестировать в dry-run?

**A:** Минимум 2 недели перед переходом на live trading.

### Q: Сколько нужно денег для начала?

**A:** Для live trading: $100-500 для начала. Но сначала протестируйте в dry-run!

### Q: Telegram обязателен?

**A:** Нет, но **настоятельно рекомендуется**. Без него вы не узнаете о проблемах оперативно.

### Q: Какую биржу выбрать?

**A:** Binance (основная), Bybit (альтернатива). Обе поддерживаются.

### Q: Что делать если API ключи не работают?

**A:** Проверьте:
1. Правильность ключей (скопированы полностью)
2. Разрешения API (Enable Reading включен)
3. IP Whitelist (если включён)
4. Время на сервере синхронизировано

```bash
# Синхронизация времени
sudo ntpdate -s time.nist.gov
```

---

## 📚 Дополнительная Документация

**Подробные руководства:**
- 🔑 Настройка API: `docs/API_SETUP_RU.md`
- 🤖 Настройка Telegram: `docs/TELEGRAM_SETUP_RU.md`
- 🚀 Полный деплой: `DEPLOYMENT.md`
- 📖 Общий README: `README.md`

**Проверка конфигурации:**
```bash
python3 scripts/validate_config.py
```

**Помощь:**
- Проблемы: GitHub Issues
- Документация Freqtrade: https://www.freqtrade.io/

---

## ⚠️ Важные Напоминания

### Безопасность

✅ **ДЕЛАЙТЕ:**
- Используйте IP Whitelist
- Включите 2FA
- Меняйте ключи каждые 3-6 месяцев
- Храните backup .env в безопасном месте

❌ **НЕ ДЕЛАЙТЕ:**
- НЕ коммитьте .env в git
- НЕ включайте Withdrawals в API
- НЕ делитесь API ключами
- НЕ храните ключи в облаках без шифрования

### Риск-Менеджмент

- Начинайте с малых сумм ($100-500)
- Тестируйте 2+ недели в dry-run
- Мониторьте ежедневно
- Используйте только "лишние" деньги
- Принимайте убытки как часть процесса

---

**Удачи в трейдинге! 🏛️**

*"Лучшая сделка - та, которую ты не совершил."*
