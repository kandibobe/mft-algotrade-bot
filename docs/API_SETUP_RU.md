# Руководство по Настройке API Ключей
# ====================================

## 📋 Содержание

1. [Binance API](#binance-api)
2. [Bybit API](#bybit-api)
3. [Безопасность API Ключей](#безопасность)
4. [Проверка Конфигурации](#проверка)

---

## 🔑 Binance API

### Шаг 1: Создание API Ключа

1. **Войдите на Binance**
   - Перейдите на https://www.binance.com/
   - Авторизуйтесь в своём аккаунте

2. **Откройте API Management**
   - Наведите на иконку профиля (правый верхний угол)
   - Выберите **API Management**
   - Или прямая ссылка: https://www.binance.com/en/my/settings/api-management

3. **Создайте новый API ключ**
   - Нажмите **Create API**
   - Выберите тип: **System Generated** (рекомендуется)
   - Введите Label: `StoicCitadel` (для идентификации)
   - Подтвердите через 2FA (Google Authenticator или SMS)

4. **Сохраните ключи**
   ```
   API Key: ваш_публичный_ключ_здесь
   Secret Key: ваш_секретный_ключ_здесь
   ```
   ⚠️ **ВАЖНО**: Secret Key показывается **только один раз**! Сохраните его немедленно.

### Шаг 2: Настройка Разрешений (Permissions)

**Для Dry-Run (бумажная торговля):**
- ✅ Enable Reading (обязательно)
- ❌ Enable Spot & Margin Trading (НЕ включать)
- ❌ Enable Withdrawals (НЕ включать)

**Для Live Trading (реальная торговля):**
- ✅ Enable Reading
- ✅ Enable Spot & Margin Trading
- ❌ Enable Withdrawals (НИКОГДА не включайте!)

### Шаг 3: IP Whitelist (Опционально, но рекомендуется)

1. Узнайте IP вашего сервера:
   ```bash
   curl ifconfig.me
   ```

2. В API Management:
   - Нажмите **Edit restrictions**
   - Включите **Restrict access to trusted IPs only**
   - Добавьте IP вашего сервера
   - Сохраните

### Шаг 4: Добавьте ключи в .env

```bash
# Откройте .env файл
nano .env

# Добавьте ваши ключи:
BINANCE_API_KEY=ваш_api_key_здесь
BINANCE_API_SECRET=ваш_api_secret_здесь
```

---

## 🔑 Bybit API (Альтернативная биржа)

### Шаг 1: Создание API Ключа

1. **Войдите на Bybit**
   - Перейдите на https://www.bybit.com/
   - Авторизуйтесь

2. **API Management**
   - Наведите на иконку профиля
   - Выберите **API**
   - Или: https://www.bybit.com/app/user/api-management

3. **Создайте API Key**
   - Нажмите **Create New Key**
   - Выберите **System-generated API Keys**
   - API Key Name: `StoicCitadel`
   - Подтвердите через 2FA

4. **Сохраните ключи**
   ```
   API Key: ваш_bybit_api_key
   Secret Key: ваш_bybit_secret_key
   ```

### Шаг 2: Настройка Разрешений

**Для Dry-Run:**
- ✅ Read
- ❌ Trade (НЕ включать)
- ❌ Withdraw (НЕ включать)

**Для Live Trading:**
- ✅ Read
- ✅ Trade
- ❌ Withdraw (НИКОГДА!)

### Шаг 3: IP Whitelist

Аналогично Binance - добавьте IP вашего сервера.

### Шаг 4: Добавьте в .env

```bash
BYBIT_API_KEY=ваш_bybit_api_key
BYBIT_API_SECRET=ваш_bybit_secret
```

---

## 🔐 Безопасность API Ключей

### ❌ НИКОГДА НЕ ДЕЛАЙТЕ

1. **НЕ коммитьте .env в git**
   ```bash
   # Проверьте, что .env в .gitignore
   cat .gitignore | grep .env
   ```

2. **НЕ включайте Withdrawals**
   - Даже если украдут ключи, средства будут в безопасности

3. **НЕ используйте одни ключи для всех ботов**
   - Создайте отдельные ключи для каждого проекта

4. **НЕ храните ключи в облаках без шифрования**
   - Dropbox, Google Drive и т.д. - небезопасно

### ✅ ОБЯЗАТЕЛЬНО ДЕЛАЙТЕ

1. **Используйте IP Whitelist**
   - Ограничьте доступ только с вашего сервера

2. **Включите 2FA**
   - Google Authenticator или Authy (безопаснее SMS)

3. **Регулярно ротируйте ключи**
   - Меняйте каждые 3-6 месяцев

4. **Мониторьте активность API**
   - Проверяйте логи на бирже на подозрительную активность

5. **Backup .env файла**
   - Храните копию в безопасном месте (не в облаке!)
   ```bash
   # Зашифрованный backup
   gpg -c .env
   # Создаст .env.gpg (зашифрованный файл)
   ```

---

## 🧪 Проверка Конфигурации

### Проверка подключения к Binance

```bash
# Запустите Freqtrade в тестовом режиме
docker-compose run --rm freqtrade list-markets \
  --exchange binance \
  --config /freqtrade/user_data/config/config_production.json
```

**Ожидаемый результат:**
```
Exchange: binance
Market: spot
Pairs: BTC/USDT, ETH/USDT, ...
```

**Если ошибка:**
- Проверьте правильность API ключей
- Проверьте IP Whitelist
- Проверьте разрешения API (Enable Reading)

### Проверка баланса

```bash
docker-compose run --rm freqtrade show-balance \
  --config /freqtrade/user_data/config/config_production.json
```

**Dry-Run режим:** Покажет виртуальный баланс из конфига
**Live режим:** Покажет реальный баланс на бирже

---

## 📝 Пример .env файла

```env
# =============================================================================
# STOIC CITADEL - ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ
# =============================================================================
# ВАЖНО: Этот файл содержит секретные данные!
# НЕ коммитьте его в git!
# =============================================================================

# -----------------------------------------------------------------------------
# BINANCE API (Основная биржа)
# -----------------------------------------------------------------------------
# Где взять: https://www.binance.com/en/my/settings/api-management
#
# Для Dry-Run (тестирование):
#   - Можно оставить пустыми или использовать тестовые ключи
#
# Для Live Trading (реальная торговля):
#   - Включите только: Enable Reading, Enable Spot Trading
#   - НИКОГДА не включайте: Enable Withdrawals
#   - Используйте IP Whitelist
#
BINANCE_API_KEY=
BINANCE_API_SECRET=

# -----------------------------------------------------------------------------
# BYBIT API (Альтернативная биржа - опционально)
# -----------------------------------------------------------------------------
# Где взять: https://www.bybit.com/app/user/api-management
#
BYBIT_API_KEY=
BYBIT_API_SECRET=

# -----------------------------------------------------------------------------
# TELEGRAM BOT (Уведомления и управление)
# -----------------------------------------------------------------------------
# Как настроить: см. docs/TELEGRAM_SETUP.md
#
# TELEGRAM_TOKEN: Получите от @BotFather
# TELEGRAM_CHAT_ID: Получите от @userinfobot
#
TELEGRAM_TOKEN=
TELEGRAM_CHAT_ID=

# -----------------------------------------------------------------------------
# FREQTRADE API (Веб-интерфейс)
# -----------------------------------------------------------------------------
# Доступ к API и FreqUI Dashboard
# По умолчанию: http://127.0.0.1:8080 и http://127.0.0.1:3000
#
FREQTRADE_API_USERNAME=stoic_admin
FREQTRADE_API_PASSWORD=StoicGuard2024!ChangeMe

# -----------------------------------------------------------------------------
# JUPYTER LAB (Исследовательская среда)
# -----------------------------------------------------------------------------
# Токен для доступа к Jupyter Lab
# По умолчанию: http://127.0.0.1:8888
#
JUPYTER_TOKEN=stoic2024

# -----------------------------------------------------------------------------
# POSTGRESQL (База данных для аналитики - опционально)
# -----------------------------------------------------------------------------
POSTGRES_USER=stoic_trader
POSTGRES_PASSWORD=StoicDB2024!ChangeMe
POSTGRES_DB=trading_analytics

# -----------------------------------------------------------------------------
# RISK MANAGEMENT (Управление рисками)
# -----------------------------------------------------------------------------
# Эти параметры также настраиваются в config_production.json
#
MAX_DRAWDOWN_PERCENT=10
MAX_OPEN_TRADES=3
STAKE_CURRENCY=USDT
DRY_RUN=true

# -----------------------------------------------------------------------------
# VPS DEPLOYMENT (Развёртывание на сервере - опционально)
# -----------------------------------------------------------------------------
# Используется для автоматического деплоя на VPS
#
VPS_IP=
VPS_USER=root
VPS_SSH_KEY=~/.ssh/id_rsa
```

---

## 🚨 Типичные Ошибки

### Ошибка: "Invalid API-key, IP, or permissions"

**Причина:**
- Неправильные API ключи
- IP не в whitelist
- Недостаточно разрешений

**Решение:**
1. Проверьте правильность ключей (скопированы полностью)
2. Проверьте IP whitelist на бирже
3. Включите "Enable Reading" в разрешениях API

### Ошибка: "Signature verification failed"

**Причина:**
- Неправильный Secret Key
- Проблемы с временем на сервере

**Решение:**
```bash
# Синхронизируйте время
sudo ntpdate -s time.nist.gov

# Или установите NTP
sudo apt install ntp
sudo systemctl start ntp
```

### Ошибка: "Market is not active"

**Причина:**
- Пара не торгуется на выбранной бирже
- Пара приостановлена биржей

**Решение:**
```bash
# Проверьте доступные пары
docker-compose run --rm freqtrade list-markets --exchange binance
```

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте `scripts/validate_config.py` (скрипт проверки конфигурации)
2. Прочитайте логи: `./scripts/citadel.sh logs freqtrade`
3. Изучите документацию Freqtrade: https://www.freqtrade.io/
4. Проверьте статус биржи: https://www.binance.com/en/support/announcement

---

**Безопасность прежде всего!** 🔐

Помните: API ключи - это ваши деньги. Относитесь к ним как к PIN-коду банковской карты.
