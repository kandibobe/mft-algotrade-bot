# 🔬 Research Lab

Эта папка для **исследований и разработки стратегий** перед их деплоем в Freqtrade.

## 📂 Содержимое

- `01_strategy_template.ipynb` - шаблон для создания новых стратегий
- `02_backtest_comparison.ipynb` - сравнение нескольких стратегий (TODO)
- `03_walk_forward_validation.ipynb` - валидация walk-forward (TODO)

## 🚀 Как использовать

```bash
# Запусти Jupyter Lab
make research

# Открой http://localhost:8888
# Токен: stoic2024
```

## 💡 Рабочий процесс

1. **Исследование** - изучи данные, тестируй идеи в ноутбуках
2. **Бэктест** - проверь стратегию на исторических данных
3. **Валидация** - убедись что результаты стабильны
4. **Имплементация** - перенеси стратегию в `user_data/strategies/`
5. **Тестирование** - запусти в dry-run режиме
6. **Деплой** - запусти live (с осторожностью!)

## 📊 Доступ к данным

Данные доступны в `/home/jovyan/user_data/data/` внутри Jupyter контейнера:

```python
import pandas as pd
from pathlib import Path

data_path = Path("/home/jovyan/user_data/data")
df = pd.read_json(data_path / "BTC-USDT-5m.json")
```

## 🧪 Библиотеки

Установлены:
- `vectorbt` - быстрый бэктестинг
- `pandas` / `numpy` - работа с данными
- `plotly` - визуализация
- `scikit-learn` - ML алгоритмы
- `xgboost` / `lightgbm` / `catboost` - градиентный бустинг
- `ta` / `ta-lib` - технические индикаторы

## ⚠️ Важно!

- **Research ≠ Production** - хорошие результаты в бэктесте не гарантируют прибыль
- **Always validate** - используй walk-forward и out-of-sample тестирование
- **Start small** - начинай с малых сумм при переходе на live
- **Monitor closely** - следи за ботом первые дни/недели

---

**🏛️ Stoic Citadel Research Lab**
