# 🎯 ИНСТРУКЦИЯ ПО ЗАПУСКУ - Stoic Citadel HFT Bot

**Дата**: 3 декабря 2024  
**Статус**: ✅ ВСЕ ГОТОВО К ЗАПУСКУ

---

## ✅ ЧТО УЖЕ СДЕЛАНО

Я выполнил полную ревизию и улучшение проекта:

### 🔧 Исправленные проблемы:

1. ✅ **Восстановлена StoicStrategyV1.py** - основная стратегия теперь на месте
2. ✅ **Обновлен docker-compose.yml** - правильная конфигурация всех сервисов
3. ✅ **Улучшены Jupyter notebooks** - полная интеграция с PostgreSQL
4. ✅ **Добавлены новые аналитические инструменты** - автоматизация бэктестов
5. ✅ **Создана документация START.md** - быстрый старт на русском

### 📚 Новые файлы:

- `START.md` - Быстрый старт (5 минут)
- `CLEANUP_GUIDE.md` - Опциональная очистка
- `research/02_strategy_analyzer.ipynb` - Анализ производительности
- `research/03_backtest_automation.ipynb` - Автоматизация тестирования
- `scripts/setup_postgres.sql` - Инициализация базы данных

---

## 🚀 ЧТО ТЕБЕ НУЖНО СДЕЛАТЬ

### Шаг 1: Клонируй обновленный репозиторий

```bash
# Если уже клонирован, обнови:
cd C:\\hft-algotrade-bot
git pull origin main

# Или клонируй заново:
git clone https://github.com/kandibobe/hft-algotrade-bot.git
cd hft-algotrade-bot
```

### Шаг 2: Создай .env файл

```bash
# Скопируй пример
cp .env.example .env
```

Открой `.env` и заполни **минимально необходимые** параметры:

```env
# ⚠️ ОБЯЗАТЕЛЬНО измени эти пароли!
FREQTRADE_API_PASSWORD=твой_сильный_пароль_123!
POSTGRES_PASSWORD=твой_db_пароль_456!

# Для первого запуска оставь пустыми (будет работать в dry-run)
BINANCE_API_KEY=
BINANCE_API_SECRET=

# Режим тестирования (виртуальные деньги)
DRY_RUN=true

# Опционально: Telegram уведомления (можно добавить позже)
TELEGRAM_TOKEN=
TELEGRAM_CHAT_ID=
```

### Шаг 3: Запусти Docker контейнеры

```bash
# Запуск всех сервисов
docker-compose up -d

# Проверь статус (все должны быть "Up")
docker-compose ps
```

Ожидаемый вывод:
```
NAME                  STATUS    PORTS
stoic_freqtrade       Up        127.0.0.1:8080->8080/tcp
stoic_frequi          Up        127.0.0.1:3000->8080/tcp
stoic_jupyter         Up        127.0.0.1:8888->8888/tcp
stoic_postgres        Up        127.0.0.1:5432->5432/tcp
stoic_portainer       Up        127.0.0.1:9000->9000/tcp
```

### Шаг 4: Проверь работу сервисов

Открой в браузере:

1. **FreqUI Dashboard**: http://localhost:3000
   - Логин: `stoic_admin`
   - Пароль: `из .env файла`
   - Здесь увидишь статус бота и сделки

2. **Jupyter Lab**: http://localhost:8888
   - Token: `stoic2024`
   - Здесь можешь анализировать данные и тестировать стратегии

3. **Portainer**: http://localhost:9000
   - Управление Docker контейнерами
   - Первый запуск попросит создать админ-аккаунт

### Шаг 5: Инициализируй PostgreSQL

```bash
# Войди в контейнер PostgreSQL
docker-compose exec postgres psql -U stoic_trader -d trading_analytics

# Выполни setup скрипт (скопируй содержимое scripts/setup_postgres.sql)
\\i /path/to/setup_postgres.sql

# Или напрямую через Docker
docker-compose exec -T postgres psql -U stoic_trader -d trading_analytics < scripts/setup_postgres.sql
```

### Шаг 6: Загрузи исторические данные

```bash
# Скачай данные для бэктеста (90 дней)
docker-compose exec freqtrade freqtrade download-data \\
  --config /freqtrade/user_data/config/config_production.json \\
  --timerange 20240901- \\
  --timeframes 5m 15m 1h \\
  --pairs BTC/USDT ETH/USDT BNB/USDT
```

### Шаг 7: Запусти первый бэктест

```bash
# Тест стратегии на исторических данных
docker-compose exec freqtrade freqtrade backtesting \\
  --strategy StoicStrategyV1 \\
  --timerange 20240901-20241203 \\
  --config /freqtrade/user_data/config/config_production.json
```

---

## 📊 ИСПОЛЬЗОВАНИЕ JUPYTER LAB

Открой http://localhost:8888 (token: `stoic2024`)

### Доступные notebooks:

1. **01_research_template.ipynb** - Шаблон для исследований
2. **02_strategy_analyzer.ipynb** - Анализ производительности стратегии
3. **03_backtest_automation.ipynb** - Автоматизация бэктестов

### Пример использования:

```python
# В Jupyter откройте 02_strategy_analyzer.ipynb
# Запустите все ячейки (Cell -> Run All)
# Получите полный анализ производительности стратегии
```

---

## 🔍 ПРОВЕРКА ЛОГОВ

```bash
# Логи Freqtrade (основной бот)
docker-compose logs -f freqtrade

# Логи всех сервисов
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f jupyter
```

---

## 🎓 ДАЛЬНЕЙШИЕ ШАГИ

### 1. Наблюдай бот в dry-run режиме (2 недели минимум)

Бот сейчас работает с **виртуальными деньгами**. Понаблюдай за его производительностью:

- Проверяй Dashboard каждый день
- Смотри какие сделки он делает
- Анализируй прибыльность в Jupyter

### 2. Получи Binance API ключи (когда будешь готов к live)

Подробная инструкция: [TODO_FOR_YOU.md](TODO_FOR_YOU.md)

**⚠️ ВАЖНО**: Для первого запуска **НЕ включай** "Enable Spot Trading"!

### 3. Настрой Telegram уведомления (опционально)

1. Создай бота через @BotFather
2. Получи Chat ID через @userinfobot
3. Добавь в `.env`:
   ```env
   TELEGRAM_TOKEN=твой_токен
   TELEGRAM_CHAT_ID=твой_chat_id
   ```
4. Перезапусти: `docker-compose restart freqtrade`

### 4. Изучи стратегии

Открой файлы:
- `user_data/strategies/StoicStrategyV1.py` - Основная стратегия
- `user_data/strategies/StoicCitadelV2.py` - Улучшенная версия
- `user_data/strategies/StoicEnsembleStrategy.py` - Ансамбль

### 5. Оптимизируй стратегию через HyperOpt

```bash
# Запуск оптимизации (это займет время!)
docker-compose exec freqtrade freqtrade hyperopt \\
  --strategy StoicStrategyV1 \\
  --hyperopt-loss SharpeHyperOptLoss \\
  --spaces buy sell roi stoploss \\
  --epochs 100
```

---

## 🛑 ОСТАНОВКА И ПЕРЕЗАПУСК

```bash
# Остановить все сервисы
docker-compose down

# Остановить и удалить данные
docker-compose down -v

# Перезапустить конкретный сервис
docker-compose restart freqtrade

# Пересобрать контейнеры после изменений
docker-compose up -d --build
```

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

### Безопасность:

- ✅ **DRY_RUN=true** всегда для тестирования
- ❌ **НИКОГДА** не включай "Withdrawals" в Binance API
- ✅ Используй **сильные пароли** в `.env`
- ❌ **НИКОГДА** не коммить `.env` в Git

### Производительность:

- Первый запуск может занять 2-3 минуты (загрузка образов)
- Загрузка данных зависит от интернета (5-10 минут)
- Бэктест на 90 днях занимает ~2-5 минут

### Ресурсы:

- Минимум 4GB RAM
- Минимум 10GB свободного места
- Docker Desktop должен быть запущен

---

## 🆘 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: Docker не запускается

```bash
# Очисти старые контейнеры
docker system prune -a
docker-compose down -v

# Перезапусти Docker Desktop
# Запусти заново
docker-compose up -d
```

### Проблема: Порт 3000 занят

```bash
# Найди процесс
netstat -ano | findstr :3000

# Останови процесс (замени PID)
taskkill /PID <номер_процесса> /F

# Или измени порт в docker-compose.yml
ports:
  - \"127.0.0.1:3001:8080\"  # Изменил 3000 на 3001
```

### Проблема: FreqUI не открывается

```bash
# Проверь логи
docker-compose logs frequi

# Перезапусти
docker-compose restart frequi

# Подожди 30 секунд и открой снова
```

### Проблема: PostgreSQL connection refused

```bash
# Проверь запущен ли контейнер
docker-compose ps postgres

# Проверь логи
docker-compose logs postgres

# Перезапусти
docker-compose restart postgres
```

---

## 📚 ПОЛЕЗНЫЕ КОМАНДЫ

```bash
# Статус всех контейнеров
docker-compose ps

# Логи с отслеживанием
docker-compose logs -f

# Войти в контейнер
docker-compose exec freqtrade bash

# Выполнить команду freqtrade
docker-compose exec freqtrade freqtrade <команда>

# Очистить логи
docker-compose logs > /dev/null

# Показать использование ресурсов
docker stats
```

---

## 🎯 ЧЕКЛИСТ ГОТОВНОСТИ

Перед переходом на **live trading** убедись:

- [ ] Бот работает в dry-run минимум 2 недели
- [ ] Win rate > 50%
- [ ] Profit factor > 1.5
- [ ] Нет критических ошибок в логах
- [ ] Telegram уведомления работают
- [ ] Binance API ключи настроены (БЕЗ withdrawals!)
- [ ] Установлен IP Whitelist на Binance
- [ ] Включена 2FA на Binance
- [ ] Понимаешь как работает стратегия
- [ ] Готов к потенциальным убыткам

---

## 📞 ПОДДЕРЖКА

- **GitHub Issues**: https://github.com/kandibobe/hft-algotrade-bot/issues
- **Документация**: См. файлы в корне проекта
- **Claude Chat**: Спроси меня если что-то непонятно!

---

## 🎉 ГОТОВО!

Если все вышеуказанные шаги выполнены - **поздравляю**! Твой торговый бот готов к работе.

**Следующий шаг**: Открой Dashboard (http://localhost:3000) и наблюдай как бот работает в тестовом режиме.

---

🏛️ **Stoic Citadel** - Trade with wisdom, not emotion.  
💪 **Keep calm and trust the process!**
